# Component Proposal Protocol

When a feature needs a reusable primitive that does not exist in `design-system/`, add a dated proposal here.

Each proposal should include:

- component name
- problem solved
- references used
- proposed states or API
- why it belongs in the shared system
- fallback if it remains feature-local
